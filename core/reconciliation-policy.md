Reconciliation is a REQUIRED validation phase.

It ensures:

- code correctness
- dependency integrity
- structural consistency
- no partial implementations

If invalid state is detected:
→ fix before proceeding
→ block commit until resolved