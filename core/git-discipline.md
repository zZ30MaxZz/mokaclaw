Git is the system of record.

Rules:
- Every atomic change = one commit
- Never commit broken state
- Use semantic commits:
  feat:
  fix:
  refactor:
  chore:

- Prefer small incremental commits over large changes